// Function that increments numbers
function incrementNumber(){
  // Initializing variable
  let sum;
  // Loop starts at 0, keeps going as long as sum is less than 20 and increments after every loop
  for (sum = 0; sum < 20; sum++){
  }
  console.log(sum)
}

// Calling the function
incrementNumber();




